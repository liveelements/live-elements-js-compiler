component X{
   fn run(){
       var a: number = 100
       let x:number = 100
       let y:string = 20
       let {m, n} = {m: 1, n: 2}
       const z:number = 100
       
       function test(c:number, d:number): string {
       
       }

       const test2 = (a:number, b:number): number => {

       }

       const test3 = s => new Array(100)

       const simple = function(a:number):number {}
       const annotated = async function(a:number):number {}
   }

   default children: this.selectedNodes?.map((n:any, level:number) => Child{
     handler: (x:any) => {
        const internalHandler = (x:any) => {
            return x + 1
        }
     }

    onReset: () => index.reset()
    onJumpToLevel: (levelInclusive:number) => index.jumpToLevel(levelInclusive)
    onSelectAtLevel: (level:number, nodeId:string) => index.selectAtLevel(level, nodeId)
    onWrapEl: (el:any) => index.setWrapEl(el)
    onOptionsRowEl: (el:any) => index.setOptionsRowEl(el)
    onSelectedEl: (level:number, el:any) => index.setSelectedEl(level, el)
   })
}