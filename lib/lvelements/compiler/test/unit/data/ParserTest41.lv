component X{
    any dExpr : '10' + Element{ var y : 10 }.toString()
    any dBlk: {
        return '10' + Element{ var y : 10 }.toString()
    }
    any dBindExpr: this.dExpr + Element{ var y : 10 }.toString()
    any dBindBlock: {
        return this.dExpr + Element{ var y : 10 }.toString()
    }

    aExpr : '10' + Element{ var y : 10 }.toString()
    aBlk: {
        return '10' + Element{ var y : 10 }.toString()
    }
    aBindExpr: this.dExpr + Element{ var y : 10 }.toString()
    aBindBlock: {
        return this.dExpr + Element{ var y : 10 }.toString()
    }
}

instance y Y{
    any dExpr : '10' + Element{ var y : 10 }.toString()
    any dBlk: {
        return '10' + Element{ var y : 10 }.toString()
    }
    any dBindExpr: this.dExpr + Element{ var y : 10 }.toString()
    any dBindBlock: {
        return this.dExpr + Element{ var y : 10 }.toString()
    }

    aExpr : '10' + Element{ var y : 10 }.toString()
    aBlk: {
        return '10' + Element{ var y : 10 }.toString()
    }
    aBindExpr: this.dExpr + Element{ var y : 10 }.toString()
    aBindBlock: {
        return this.dExpr + Element{ var y : 10 }.toString()
    }
}

