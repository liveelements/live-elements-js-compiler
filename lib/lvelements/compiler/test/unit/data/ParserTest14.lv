import test
import language

instance default TestCase{

    LanguageScanner ls: LanguageScanner{
        number x : 0
    }

    Scenario{
        run : (t) => {
            var ls = new LanguageScanner()
        }
    }
}
